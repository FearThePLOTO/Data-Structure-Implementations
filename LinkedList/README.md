# Java Linked List Implementation

This repository contains a simple implementation of a **singly linked list** data structure in Java.  
The `LinkedList` class is built completely from scratch without using Java’s built-in `LinkedList`, making it perfect for understanding how linked lists work internally.

---

## 📂 Package
```
LinkedListImp.src
```

---

## 📌 Features
- ✅ Insert elements at the end of the list  
- ✅ Delete elements by value  
- ✅ Access elements by index (`get`)  
- ✅ Modify elements by index (`set`)  
- ✅ Search for an element (`contains`, `indexOf`)  
- ✅ Check if the list is empty  
- ✅ Get current size  
- ✅ Clear all elements  
- ✅ Display all elements in a readable format  
